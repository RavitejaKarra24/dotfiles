# Keep only local overrides. Yazi supplies and evolves its default keymap.

[mgr]
prepend_keymap = [
  { on = [ "g", "c" ], run = "cd ~/.config",   desc = "Go to ~/.config" },
  { on = [ "g", "d" ], run = "cd ~/Downloads", desc = "Go to Downloads" },
]
